import type { Embed, EmbedBlogProps } from '~/components/PageBlocks/Embed/interfaces/IEmbedBlog'

export interface EmbedCustomPageProps {
  items: Embed[]
}

export interface IEmbedCustomPage {
  slice: EmbedBlogProps
}
